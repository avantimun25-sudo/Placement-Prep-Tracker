## Packages
recharts | Data visualization for progress charts
framer-motion | Smooth animations and page transitions
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind classes safely

## Notes
- Images should be from Unsplash with descriptive alt text
- Resume upload is a mock interface (UI only)
- Real-time updates handled via React Query cache invalidation
